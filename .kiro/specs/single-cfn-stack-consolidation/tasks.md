# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Missing Consolidated Template and DependsOn Chain
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the structural gap that forces manual deployment steps
  - **Scoped PBT Approach**: The bug is deterministic (structural absence), so scope the property to the concrete failing cases: absence of `cloudformation/iceberg-pipeline.yaml`, absence of `GlueJobTriggerCustomResource` in any existing template, and absence of a `DependsOn` on `GlueJobTriggerCustomResource` in `IcebergFirehoseStream`
  - Create `tests/test_bug_condition.py` with the following checks:
    - Assert that `cloudformation/iceberg-pipeline.yaml` does NOT exist (confirms the consolidated template is missing)
    - Parse `cloudformation/iceberg-pipeline-firehose.yaml` and assert that `IcebergFirehoseStream.DependsOn` does NOT contain any reference to a Glue job trigger resource (confirms the missing dependency)
    - Parse all existing templates and assert that none contains a resource of type `AWS::CloudFormation::CustomResource` (confirms the missing orchestration layer)
    - Parse all existing templates and assert that none contains an `AWS::Lambda::Function` with Glue trigger logic (i.e., no function whose inline code references `glue.start_job_run`)
  - Run test on UNFIXED code (before creating `cloudformation/iceberg-pipeline.yaml`)
  - **EXPECTED OUTCOME**: Tests PASS on unfixed code (this is correct — they prove the bug exists by confirming the structural gaps)
  - Document counterexamples found: "No `GlueJobTriggerCustomResource` exists; `IcebergFirehoseStream` has no dependency on a Glue job completion signal; no consolidated template exists"
  - Mark task complete when test is written, run, and the structural gaps are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing Resource Configurations Are Unchanged
  - **IMPORTANT**: Follow observation-first methodology — observe behavior on UNFIXED code for non-buggy inputs (runtime resource configurations), then write tests that assert those observed configurations
  - Create `tests/test_preservation.py` with property-based tests using `hypothesis`:
    - **Firehose configuration preservation**: For any valid parameter combination, assert that `IcebergFirehoseStream` in the consolidated template has the same `IcebergDestinationConfiguration` structure (catalog ARN pattern, database name ref, table name ref, buffering hints refs, S3 configuration, retry options) as `iceberg-pipeline-firehose.yaml`
    - **Lambda function preservation**: Assert that `CloudWatchLogsToIcebergFunction` in the consolidated template has the same runtime (`python3.11`), handler (`lambda_function.lambda_handler`), environment variable (`FIREHOSE_STREAM_NAME`), DLQ config, and S3 code reference (`lambda/lambda_function.zip`) as the original
    - **DLQ preservation**: Assert that `FailedEventsQueue` and `PermanentFailureQueue` SQS queues are present with the same `MessageRetentionPeriod`, `SqsManagedSseEnabled`, and `RedrivePolicy` configuration
    - **Parameter defaults preservation**: For all parameters shared between the original templates and the consolidated template, assert that defaults are identical (e.g., `IcebergDataBucketName=my-company-iceberg-data-bucket`, `Region=us-east-1`, `GlueJobName=my-iceberg-glue-job`, `WorkerType=G.1X`, `NumberOfWorkers=2`)
    - **Glue job Spark config preservation**: Assert that the `--conf` block in `IcebergGlueJob.DefaultArguments` contains all 12 required Spark properties (extensions, catalog type, warehouse path, region, account ID, etc.)
    - **S3 bucket public access preservation**: Assert that `IcebergDataBucket` and `IcebergErrorsBucket` both have all four `PublicAccessBlockConfiguration` settings set to `true`
  - Observe behavior on UNFIXED templates (`iceberg-pipeline-firehose.yaml`, `iceberg-pipeline-glue.yaml`) to record baseline values before writing assertions
  - Run tests on UNFIXED code (they will fail because `iceberg-pipeline.yaml` does not exist yet — this is expected and documents the baseline)
  - **EXPECTED OUTCOME**: Tests FAIL on unfixed code because the consolidated template does not exist yet (this is correct — they define the preservation contract)
  - Mark task complete when tests are written and the preservation contract is documented
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Create consolidated CloudFormation template and verify all tests pass

  - [x] 3.1 Create `cloudformation/iceberg-pipeline.yaml` — merge all three templates
    - Merge `iceberg-pipeline-glue.yaml`, `iceberg-pipeline-glue-no-lakeformation.yaml`, and `iceberg-pipeline-firehose.yaml` into a single file
    - Carry over all parameters from both source templates (AccountId, Region, DatabaseName, IcebergDataBucketName, IcebergErrorsBucketName, GlueScriptBucketName, GlueJobRoleName, GlueJobName, WorkerType, NumberOfWorkers, WarehouseDatabaseName, FirehoseStreamName, FirehoseBufferSizeMBs, FirehoseBufferIntervalSeconds, StreamDatabaseName, StreamTableName, LambdaFunctionName, LambdaScriptBucketName, LambdaTimeout, LambdaMemorySize, SubscriptionLogGroupName, CreateSubscriptionLogGroup, LambdaFirehoseProcessorRoleName, FirehoseIcebergDeliveryRoleName)
    - Add `EnableLakeFormation` parameter: Type String, AllowedValues ["true","false"], Default "true", Description "Enable Lake Formation integration for the Glue job and S3 bucket policy"
    - Add `GlueJobTriggerFunctionName` parameter: Type String, Default "iceberg-glue-job-trigger", Description "Name for the Lambda function that triggers and polls the Glue job"
    - Add `Conditions` section with `LakeFormationEnabled: !Equals [!Ref EnableLakeFormation, "true"]` and `ShouldCreateSubscriptionLogGroup: !Equals [!Ref CreateSubscriptionLogGroup, "true"]`
    - Carry over all S3 bucket resources (`IcebergDataBucket`, `IcebergErrorsBucket`) with identical `PublicAccessBlockConfiguration`
    - Add `IcebergDataBucketPolicy` using `!If [LakeFormationEnabled, <lf-statement>, <no-lf-statement>]` to switch between the Lake Formation service role principal (from `iceberg-pipeline-glue.yaml`) and the Glue job role principal (from `iceberg-pipeline-glue-no-lakeformation.yaml`)
    - Add `GlueJobRole` with conditional Lake Formation inline policy: include `lake-formation-access` policy only when `LakeFormationEnabled` is true (use `!If` on the Policies list or wrap the policy statements)
    - Add `IcebergGlueJob` with `--conf` using `!If [LakeFormationEnabled, "true", "false"]` for the `lakeformation-enabled` Spark config value
    - _Bug_Condition: isBugCondition(deploymentAttempt) where templateCount > 1 AND requiresManualGlueJobTrigger = true_
    - _Expected_Behavior: single aws cloudformation deploy completes full pipeline unattended_
    - _Preservation: all resource configs from original templates preserved unchanged_
    - _Requirements: 2.1, 3.1, 3.2, 3.5, 3.6, 3.7_

  - [x] 3.2 Add `GlueJobTriggerRole` IAM role to the consolidated template
    - Type: `AWS::IAM::Role`
    - Trust principal: `lambda.amazonaws.com`
    - Managed policy: `arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole`
    - Inline policy `GlueJobAccess` with two statements:
      - `glue:StartJobRun` on `!Sub arn:aws:glue:${Region}:${AccountId}:job/${GlueJobName}`
      - `glue:GetJobRun` on `!Sub arn:aws:glue:${Region}:${AccountId}:job/${GlueJobName}/jobrun/*`
    - _Requirements: 2.2, 2.3_

  - [x] 3.3 Add `GlueJobTriggerFunction` Lambda to the consolidated template
    - Type: `AWS::Lambda::Function`
    - Runtime: `python3.12`, Handler: `index.handler`, Timeout: `900`
    - Role: `!GetAtt GlueJobTriggerRole.Arn`
    - FunctionName: `!Ref GlueJobTriggerFunctionName`
    - Code: inline `ZipFile` with the following logic:
      - Import `boto3`, `json`, `urllib.request`, `time`
      - On `Create` and `Update` events: call `glue.start_job_run(JobName=event['ResourceProperties']['GlueJobName'])`, then poll `glue.get_job_run()` every 30 seconds until state is `SUCCEEDED`, `FAILED`, `ERROR`, `TIMEOUT`, or `STOPPED`; send `SUCCESS` or `FAILED` to the CloudFormation pre-signed URL
      - On `Delete` events: send `SUCCESS` immediately
      - Catch all exceptions and send `FAILED` signal with error message to prevent CloudFormation from waiting for its 1-hour timeout
    - _Bug_Condition: isBugCondition(deploymentAttempt) where requiresManualGlueJobTrigger = true_
    - _Expected_Behavior: Lambda polls Glue job until SUCCEEDED, then signals CloudFormation SUCCESS_
    - _Requirements: 2.2, 2.3, 2.4_

  - [x] 3.4 Add `GlueJobTriggerCustomResource` to the consolidated template
    - Type: `AWS::CloudFormation::CustomResource`
    - `ServiceToken: !GetAtt GlueJobTriggerFunction.Arn`
    - Properties passed to Lambda: `GlueJobName: !Ref GlueJobName`
    - `DependsOn: IcebergGlueJob` — ensures the Glue job resource exists before the Custom Resource triggers it
    - _Bug_Condition: isBugCondition(deploymentAttempt) where firehoseCreatedBeforeGlueTableExists = true_
    - _Expected_Behavior: Custom Resource blocks CloudFormation until Glue job reaches SUCCEEDED_
    - _Requirements: 2.2, 2.3, 2.5_

  - [x] 3.5 Add `DependsOn: GlueJobTriggerCustomResource` to `IcebergFirehoseStream`
    - Modify the `IcebergFirehoseStream` resource in the consolidated template to include `GlueJobTriggerCustomResource` in its `DependsOn` list (alongside the existing `FirehoseIcebergDeliveryRole` and `FirehoseLogStream` dependencies)
    - This is the critical fix: it guarantees the Glue Catalog table `stream_analytics.application_logs` exists before Firehose resource creation begins
    - _Bug_Condition: isBugCondition(deploymentAttempt) where firehoseCreatedBeforeGlueTableExists = true_
    - _Expected_Behavior: IcebergFirehoseStream only created after GlueJobTriggerCustomResource signals SUCCESS_
    - _Preservation: IcebergDestinationConfiguration, buffering hints, S3 config, retry options all unchanged_
    - _Requirements: 2.5, 3.2_

  - [x] 3.6 Add fix verification tests in `tests/test_consolidated_template.py`
    - Test that `cloudformation/iceberg-pipeline.yaml` is valid YAML and parseable
    - Test that `IcebergFirehoseStream.DependsOn` contains `GlueJobTriggerCustomResource`
    - Test that `GlueJobTriggerCustomResource.DependsOn` contains `IcebergGlueJob` (or is set via `DependsOn`)
    - Test that `GlueJobTriggerFunction` has `Timeout: 900`
    - Test that `GlueJobTriggerRole` has `glue:StartJobRun` and `glue:GetJobRun` in its inline policy
    - Test that `EnableLakeFormation` parameter exists with `AllowedValues: ["true", "false"]` and `Default: "true"`
    - Test that `LakeFormationEnabled` condition is defined and references `EnableLakeFormation`
    - Test that when `EnableLakeFormation=true` the `IcebergDataBucketPolicy` includes the Lake Formation service role principal
    - Test that when `EnableLakeFormation=false` the `IcebergDataBucketPolicy` includes the Glue job role principal
    - Test that all resources from `iceberg-pipeline-firehose.yaml` are present in the consolidated template (IcebergFirehoseStream, CloudWatchLogsToIcebergFunction, LambdaFirehoseProcessorRole, FirehoseIcebergDeliveryRole, FailedEventsQueue, PermanentFailureQueue, DLQEventSourceMapping, IcebergStreamingSubscriptionFilter)
    - Test that all resources from `iceberg-pipeline-glue.yaml` are present in the consolidated template (IcebergDataBucket, IcebergErrorsBucket, GlueJobRole, IcebergGlueJob)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.5, 3.6_

  - [x] 3.7 Add property-based tests in `tests/test_custom_resource_lambda.py`
    - Use `hypothesis` to generate random Glue job state sequences and verify the Custom Resource Lambda inline code signals the correct CloudFormation response:
      - **Property**: For any sequence of `RUNNING` states followed by `SUCCEEDED`, the handler sends `SUCCESS`
      - **Property**: For any sequence of `RUNNING` states followed by `FAILED`, `ERROR`, `TIMEOUT`, or `STOPPED`, the handler sends `FAILED`
      - **Property**: For any `Delete` event (regardless of Glue job state), the handler always sends `SUCCESS`
    - Use `hypothesis` to generate random `EnableLakeFormation` values (`"true"` / `"false"`) and verify the consolidated template's `LakeFormationEnabled` condition resolves correctly
    - Use `hypothesis` to generate random combinations of template parameter values (string names, numeric sizes) and verify the consolidated template YAML remains parseable and all `!Ref` / `!Sub` references resolve
    - _Requirements: 2.2, 2.3, 2.4, 3.5, 3.6_

  - [x] 3.8 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Consolidated Template Exists with Full DependsOn Chain
    - **IMPORTANT**: Re-run the SAME test from task 1 (`tests/test_bug_condition.py`) — do NOT write a new test
    - The test from task 1 encodes the expected behavior: consolidated template exists, `GlueJobTriggerCustomResource` exists, `IcebergFirehoseStream` depends on it
    - When this test passes, it confirms the expected behavior is satisfied
    - Run: `pytest tests/test_bug_condition.py -v`
    - **EXPECTED OUTCOME**: Tests FAIL (because the assertions check for the ABSENCE of the fix — the test was written to confirm the bug, not the fix). If the test was written to assert the bug condition exists (structural gaps), it will now fail because the gaps are filled. Review and update `test_bug_condition.py` assertions to assert the PRESENCE of the fix (consolidated template exists, Custom Resource exists, DependsOn chain is correct) so the test validates the fix.
    - _Requirements: 2.1, 2.2, 2.3, 2.5_

  - [x] 3.9 Verify preservation tests still pass
    - **Property 2: Preservation** - All Existing Resource Configurations Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 (`tests/test_preservation.py`) — do NOT write new tests
    - Run: `pytest tests/test_preservation.py -v`
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions — all original resource configurations are preserved in the consolidated template)
    - Confirm all preservation tests pass after the consolidated template is created
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Checkpoint — Ensure all tests pass
  - Run the full test suite: `pytest tests/ -v`
  - Ensure all tests pass, including:
    - `tests/test_bug_condition.py` (bug condition exploration — now validates the fix)
    - `tests/test_preservation.py` (preservation property tests)
    - `tests/test_consolidated_template.py` (fix verification tests)
    - `tests/test_custom_resource_lambda.py` (property-based tests for Lambda logic)
    - All pre-existing tests (`test_template_structure.py`, `test_template_integrity.py`, `test_glue_job.py`, `test_iam_role.py`, `test_s3_buckets.py`, etc.)
  - Ask the user if any questions arise about test failures or ambiguous behavior
